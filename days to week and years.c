#include <stdio.h>
main()
{
float d,w,y;
printf("enter the number of days");
scanf("%f",&d);
w=d/7;
printf("number of weeks are %f",w);
y=d/365;
printf("number of years are %f",y);
return 0;
}
